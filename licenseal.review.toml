# Generated by `licenseal init-review-file`.
# Fill in `license` with a reviewed SPDX ID, SPDX expression, or `Proprietary`.

# detected: MPL-2.0
# status: warning
[[review]]
ecosystem = "python"
package = "certifi"
version = "2026.5.20"
license = "MPL-2.0"
note = "Apache-2.0 library distributed via PyPI; certifi is an unmodified upstream dependency used with dynamic linkage. MPL-2.0 is file-level weak copyleft that only triggers on modification of the MPL-licensed files — no modifications, so the obligation is dormant."

# detected: Apache-2.0 AND CNRI-Python
# status: unknown
[[review]]
ecosystem = "python"
package = "regex"
version = "2026.5.9"
license = "Apache-2.0 AND CNRI-Python"
note = "Flagged UNKNOWN only because licenseal cannot classify the compound expression. CNRI-Python is an OSI-approved permissive license (the historical Python license) and is compatible with Apache-2.0; regex is used unmodified with dynamic linkage. Detection-classification override."

# detected: MIT AND MPL-2.0
# status: warning
[[review]]
ecosystem = "python"
package = "tqdm"
version = "4.68.1"
license = "MIT AND MPL-2.0"
note = "Same rationale as certifi: unmodified upstream dependency, dynamic linkage. The MPL-2.0 portion is file-level weak copyleft and does not trigger absent modification of the MPL-licensed files."

# detected: UNKNOWN
# status: unknown
[[review]]
ecosystem = "python"
package = "wtpsplit-lite"
version = "0.2.0"
license = "MIT"
note = "PyPI package ships no license metadata (hence UNKNOWN); the repository LICENSE file is MIT, confirmed via GitHub's license-detection API for the superlinear-ai/wtpsplit-lite repo (spdx_id MIT). Detection override — MIT is permissive and compatible with Apache-2.0."
